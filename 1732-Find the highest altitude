class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int highest=0;
        int curr=0;
        for(int x:gain){
            curr+=x;
            highest=max(curr,highest);
        }
        return highest;
    }
};
